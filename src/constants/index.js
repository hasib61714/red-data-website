// px offset when scrolling to anchor sections (topbar 36px + navbar 56px)
export const NAVBAR_HEIGHT_PX = 92

// Hero carousel timing
export const HERO_SLIDE_INTERVAL_MS   = 5000
export const HERO_SLIDE_TRANSITION_MS = 800

// Scroll-reveal IntersectionObserver threshold
export const REVEAL_THRESHOLD = 0.15
